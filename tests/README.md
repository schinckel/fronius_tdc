"""Tests for fronius_tdc integration.

Running the tests:

1. Install test dependencies:
   pip install -r test-requirements.txt

2. Run all tests:
   pytest

3. Run specific test file:
   pytest tests/test_coordinator.py

4. Run with coverage:
   pytest --cov=custom_components/fronius_tdc

5. Run specific test:
   pytest tests/test_coordinator.py::TestFroniusTDCCoordinator::test_coordinator_initialization
"""
